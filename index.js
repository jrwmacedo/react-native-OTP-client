//Import a library to help create a component
import React from 'react';
import { Text, AppRegistry } from 'react-native';
import App from './App';

//Create a component

//Render it to the device
AppRegistry.registerComponent('authenticator', () => App);
